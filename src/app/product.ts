export class Product {
    public productimage: string;
    public productIndex: string;
    constructor(
        public price: number,
        public name: string,
        public description: string
      ) {  }
    
}
